package com.mike.cpsc223j.lectures;

public class Lecture08 {
	public static void main (String[] args) {
		// Linked List
		// Data: 3 15 6
		// 1. Insert a new item in FRONT of the list
		// LIFO = stack
		// 2. Insert a new item at the REAR of the list
		// FIFO = queue
		// 3. Insert a new item, but make sure the list becomes ordered after each insertion
		// ordered linked list
		
		// No pointers in Java. see class "node"
		
		// Delete the first node
		// node p = list;
		// x = p.info
		// list = p.next
		// p = null // same as delete p in c++
		// Look at "LinkedList.java" which also uses "node.java"
		
		// delete the first node
		// node p = list;
		// list = p.next;
		// p = null; // same as 'delete p' in c++
				
				
		
	}
}